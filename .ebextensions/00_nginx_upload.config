files:
  "/etc/nginx/conf.d/upload_size.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      # Increase upload size limit to 10MB for image uploads
      # This matches the Django DATA_UPLOAD_MAX_MEMORY_SIZE setting
      client_max_body_size 10M;
